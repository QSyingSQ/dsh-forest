# dsh-forest bundle patch: inserts its dsh.client row into the web plugin
# roster. Same shape as the official dsh plugin bundles — install with:
#   dsh plugin --profile <name> add <path-or-git-url>
- insert:
    - id: dsh-forest
      name: dsh-forest

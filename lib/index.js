/**
 * dsh-skin-typora-forest — host half.
 *
 * The skin is a pure client plugin: all theming happens in the browser
 * (lib/client.js). The host half exists so the composition row loads
 * cleanly and provides no services.
 */
export function apply() {}
